# DermaTriage Documentation Gap / Assessment Register — R8

New numbered gaps: `NONE`.

Documented:

```text
two distinct adaptation paths
independent accumulation conditions
prompt threshold = 10 relevant correction elements
classifier threshold = 50 qualifying corrections
DermaTriage activation responsibility
FR-05 consumes disagreement evidence qualified by FR-07
```

Deferred:

```text
classifier disagreement qualification -> FR-07
later adaptation qualification/acceptance/lifecycle rules
```

NOT_DOCUMENTED_UNASSESSED:

```text
one-shot vs repeated trigger
count reset semantics
exact comparator/threshold-crossing semantics
deduplication
accumulation storage
cycle concurrency/instance semantics
```

No automatic gap under O-09.
