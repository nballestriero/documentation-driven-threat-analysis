# PR-01 `perform` Working-Hypothesis Question Pack R1

## Status

`REVIEW TOOL / NON-NORMATIVE / CURRENT HYPOTHESIS NOT ADMITTED`

```text
candidateId: PR-01 working hypothesis only
candidateName: perform
empiricalStatus: IRREDUCIBILITY_NOT_DEMONSTRATED
```

## Questions

1. Does the source govern an independently meaningful actor-to-behavior relation, or merely an input-to-output function?
2. Is the behavior itself a governed referent rather than a noun created from a source verb?
3. Which source anchor loses meaning if `perform` is deleted?
4. Can `produce(actor,input,result)` preserve the material meaning?
5. Can `observe`, `transition`, `decisionRule`, `transfer`, `realize`, `assignResponsibility`, `consumeService` or `constrain` preserve the relevant facet?
6. Does composition make actor-to-behavior identity recoverable?
7. Does `perform` merely duplicate a process already used as `produce.actor`?
8. Is the operation only `y = f(x)`?
9. If a result is assigned back, does the source actually govern `x_after != x_before`?
10. Does execution imply in-place mutation? Do not assume it.
11. Does execution imply a new identity? Do not assume it.
12. Does execution imply lifecycle transition? Do not assume it.
13. Are operation-target/effect-scope semantics the real missing meaning?
14. What independent query requires `perform` after inputs, outputs, effects and responsibilities are represented?
15. Can a governed behavior exist without output/result or lifecycle effect?
16. Is performer identity independently required by threat analysis, QA or projection?

## Boundaries

```text
perform != produce unless irreducible actor-to-behavior meaning remains
perform != realize
perform != assignResponsibility
perform != invoke
perform != trigger
```

## Current disposition

```text
PR-01 RETAIN
PERFORM WORKING HYPOTHESIS NOT ADMITTED
IRREDUCIBILITY NOT DEMONSTRATED
NEW TOP-LEVEL OPERATOR NOT JUSTIFIED
```
