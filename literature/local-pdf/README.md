# Local source PDFs

Keep personally downloaded source PDFs in this directory. PDF files here are ignored by Git.
Record the legal landing URL, PDF URL, retrieval date, local filename and SHA-256 in
`../source-access.registry.yml`.

Do not commit copyrighted source PDFs. Generated DDTA worksheets belong under
`../worksheets/` instead.
