# DDTA R25 - Base Analysis Rebuild R2 Consolidation Checkpoint R1

**Status:** REPOSITORY CONSOLIDATION CHECKPOINT / NO BA AUTHORITY PROMOTION

**Date:** 2026-09-18

**Input repository baseline:** `e88ebee4be20246e1af6f2b37dd402c848ed22f3`

**Repository:** `nballestriero/documentation-driven-threat-analysis`

## 1. Purpose

This checkpoint consolidates the start of the new Base Analysis guide rebuild together with the active forward work plan and the mandatory source-preservation register.

It adds, without overwriting historical predecessors:

- `methodology/DDTA_BASE_ANALYSIS_GUIDE_REBUILD_R2.{tex,pdf}`;
- `methodology/DDTA_BASE_ANALYSIS_GUIDE_REBUILD_R2_PAGE_MD5_INDEX.txt`;
- `methodology/DDTA_R25_BASE_ANALYSIS_GUIDE_REBUILD_WORK_PLAN_R2.md`;
- `methodology/DDTA_R25_BASE_ANALYSIS_REBUILD_SOURCE_REGISTER_R1.md`.

This checkpoint itself records their joint repository state.

## 2. Authority boundary

The Rebuild R2 guide is a `CANDIDATE / NON-NORMATIVE / CUMULATIVE REBUILD` artifact. This checkpoint does not promote it to BA method authority.

Current BA authority remains the existing R3 / BA0-BA5 stack until a later explicit promotion checkpoint.

Recency, compilation, repository presence or successful application to DermaTriage do not by themselves change authority.

## 3. Active forward plan

`DDTA_R25_BASE_ANALYSIS_GUIDE_REBUILD_WORK_PLAN_R2.md` becomes the active forward plan for the Base Analysis rebuild and its parallel DermaTriage application.

Older forward plans remain repository history and research genealogy. They do not govern the BA rebuild when they conflict with this checkpointed R2 plan.

The immediate sequence after this consolidation is:

```text
Section 1 - Che cos'e' la Base Analysis
    -> governed semantic fact
    -> BAReferent
    -> BAProposition
    -> immediate application to consolidated DermaTriage documentation
```

No top-level operator review begins before these foundations are reviewed sufficiently for application.

## 4. Definitive-guide boundary

The definitive guide begins with the BA method itself.

The first numbered section is:

```text
1. Che cos'e' la Base Analysis
```

Rebuild governance material is placed after the normative body in a temporary red-marked appendix.

The temporary appendix currently preserves:

- rebuild lineage and authority bookkeeping;
- source-preservation and epistemic-state rules;
- the Construct Description Contract;
- the Construct Description Contract revision/regression rule;
- the current rebuild roadmap.

Before a future definitive guide is promoted, this appendix must be removed from the guide. Any rule that proves to be part of the BA method must first be transferred explicitly to the appropriate normative section. Research genealogy remains in repository checkpoints/evidence rather than in the final guide.

## 5. Construct Description Contract

The temporary rebuild appendix contains the current fixed Construct Description Contract for individual construct review.

The contract must not be changed locally for one convenient case. If a later construct exposes an insufficiency:

```text
counterexample
    -> record missing descriptive need
    -> test generality
    -> revise the contract explicitly
    -> assign a new contract revision
    -> regression-check constructs already reviewed
```

This protects descriptive consistency during the iterative rebuild.

## 6. Source-preservation register

`DDTA_R25_BASE_ANALYSIS_REBUILD_SOURCE_REGISTER_R1.md` is mandatory input to the rebuild.

It preserves the requirement to inspect current authority, R42 candidate surfaces, historical BA0-BA6 derivation material, R24 pressure notes, DermaTriage operator/construct reviews, candidate constructs, rejected hypotheses, deferred deltas and open pressures before a construct is considered reviewed.

The register is research input, not independent BA authority.

## 7. View / projection direction

Rebuild R2 also preserves the initial direction:

```text
Governed Documentation
    -> Base Analysis
        -> BA View / Projection Contract
            -> textual/graphical renderer such as Mermaid
```

A renderer does not create BA or project meaning. Views must be derived from accepted BA semantics under explicit scope, coverage and projection rules.

The view/projection mechanism remains subject to later construct-by-construct and application review.

## 8. Current stop point

This checkpoint does not:

- start threat analysis;
- promote Rebuild R2;
- admit `provideService`, `storedIn`, `initiate` or `selection` as current BA2 vocabulary;
- close open PR/CL/CMD/OBS items;
- begin a new top-level operator review;
- alter the consolidated DermaTriage project documentation.

The next activity is the review of Section 1 and the first BA foundations under the active R2 work plan.
